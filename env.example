# Flask Configuration
FLASK_ENV=production
SECRET_KEY=your_secret_key_here

# Note: No API credentials required!
# This dashboard uses Yahoo Finance (yfinance) for historical data
# which is free and doesn't require API keys.

# Railway Configuration
PORT=5000
